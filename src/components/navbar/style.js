import styled from "styled-components";

export const NavbarContainer = styled.div`
  .nav-container {
    background-color: #1a1818;
    padding: 0.6rem;
    display: flex;
    flex-direction: row;
  }
`;
