import styled from "styled-components";

export const NavbarMenuContainer = styled.div`
  .nav-content {
    font-size: 1.4rem;
    margin: 0 20px;
    font-weight: bold;
  }

  .nav-content:hover {
    color: #08cf90;
    cursor: pointer;
  }
`;
